<%= form_for(@treatment_note) do |f| %>
  <% if @treatment_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@treatment_note.errors.count, "error") %> prohibited this treatment_note from being saved:</h2>

      <ul>
      <% @treatment_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :treatment_id %><br>
    <%= f.number_field :treatment_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
